#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_generate_name() {
        // You can write tests for the name generation here
        // For example, check if the generated name is not empty or has expected format
        // assert!(...);
    }

    #[test]
    fn test_generate_address() {
        // You can write tests for the address generation here
        // For example, check if the generated address is not empty or has expected format
        // assert!(...);
    }

    #[test]
    fn test_generate_phone() {
        // You can write tests for the phone number generation here
        // For example, check if the generated phone number is not empty or has expected format
        // assert!(...);
    }
}